# Copy to .env for local; set on Fly via `fly secrets set`

DATABASE_URL="file:./dev.db"

# Required for hosted ChatGPT Connect + Studio UI gate
NOTICED_OWNER_PASSWORD=
NOTICED_SESSION_SECRET=
NOTICED_PUBLIC_ORIGIN=http://localhost:3004

# Display name for activity attribution
NOTICED_OWNER_NAME=Owner
